services:
  api:
    build: .
    container_name: esturide-api
    hostname: esturide-api
    image: esturide-api:latest
    restart: always
    ports:
      - "8000:80"
    volumes:
      - .:/app
      - .:/docs
    networks:
      - net

volumes:
  docs:

networks:
  net:
    driver: bridge
